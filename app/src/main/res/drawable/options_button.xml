<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true"
        android:drawable="@drawable/options_pressed"/> <!-- pressed state -->
    <item android:state_focused="true"
        android:drawable="@drawable/options"/> <!-- focused state -->
    <item android:state_hovered="true"
        android:drawable="@drawable/options" /> <!-- hovered -->
    <item android:state_enabled="false"
        android:drawable="@drawable/options" /> <!-- disabled -->
    <item android:drawable="@drawable/options"/> <!-- default state -->
</selector>